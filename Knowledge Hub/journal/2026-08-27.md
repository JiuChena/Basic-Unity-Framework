---
tags: [Unity, Framework, Gameplay, Ability, Movement, Serialization, Bugfix]
created: 2026-08-27
updated: 2026-08-27
---

# 2026-08-27 修复移动能力资产引用

## 完成

- 定位 `MovementAbility.asset` 的 `Broken text PPtr` 根因：资产保存了场景 `Camera` 的本地文件标识 `330585545`，`ScriptableObject` 不能持有场景对象引用。
- 删除移动能力资产中的场景相机字段和失效引用，`MovementAbilityRuntime.Start` 改为运行时缓存 `Camera.main`。
- 确认浮动胶囊、跳跃、边缘保护没有丢失，三者现在作为 `MovementAbility.asset` 内部的 `FloatingCapsuleModule`、`JumpModule` 和 `EdgeProtectionModule` 配置存在。
- 确认 `Preload` 场景只引用输入监听能力和移动能力，没有残留三份已删除独立能力资产的引用。

## 验证待办

- 已完成静态资产与代码引用检查；未使用 Unity CLI。需要在 Unity 主编辑器重新导入资产后确认 Inspector 能显示移动、浮动胶囊、跳跃和边缘保护分组，并确认 Console 不再报告 PPtr 错误。
